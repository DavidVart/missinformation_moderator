import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.projectveritas.mobile",
  appName: "Project Veritas",
  webDir: "dist/mobile/browser",
  bundledWebRuntime: false
};

export default config;
